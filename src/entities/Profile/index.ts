import { profileAction, profileReducer } from './model/slice/profileSlice'
import { Profile, ProfileSchema } from './model/types/profileSchema'

export {Profile, ProfileSchema, profileAction, profileReducer}